# 远程操作说明

## 管理范围

- 本仓库通过 `inventory/hosts.yml` 管理操作者 SSH 配置中的所有具体主机别名。SSH
  别名新增或删除时，必须同步更新 `managed` 组。
- 对可重复操作或多主机变更使用 Ansible。仅针对明确目标开展只读诊断时，可以直接
  使用 SSH。
- 不得硬编码 IP 地址、SSH 用户、堡垒机信息、密码、令牌或私钥路径。SSH 连接信息
  应保存在操作者的 `~/.ssh/config` 中。

## 安全要求

- 状态检查、日志查看、inventory 列举、磁盘检查、进程查看和版本查询均视为只读
  操作。
- 修改服务器前，先检查当前状态并明确目标主机。
- 会产生变更的 playbook 必须先在一台主机上执行，验证成功后才能扩展到主机组。
  对任何硬件组执行变更时使用 `serial: 1`。
- 相关模块支持检查模式时，使用 `--check --diff`。
- 不得使用范围过大的破坏性命令、关闭 SSH 主机密钥检查、覆盖非预期文件或掩盖验证
  失败。
- 删除数据、替换本仓库之外已有配置、重启机器，或停止当前模型适配范围之外的服务前，
  必须取得用户明确确认。在正在进行的模型适配中，对于由 Agent 为当前任务启动，或由
  用户明确纳入本次适配范围的推理服务或相关进程，Agent 可以按需停止或重启，无需每次
  重复请求确认。适配容器本身必须保持运行，禁止停止、重启或删除。操作前必须准确确认
  服务或进程，不得影响共享或无关负载，并记录原因、命令和验证结果。
- 优先使用具备幂等性的 Ansible 模块，而不是 shell 命令。确需使用命令时，应有意识
  地定义 `changed_when` 和 `failed_when`。

## 加速卡查询

- 不得假设所有主机都使用 NVIDIA 工具。应使用平台组变量和
  `./scripts/accelerator-check`。
- NVIDIA：`nvidia-smi`。
- 华为昇腾：`npu-smi info`。
- 平头哥 PPU：`ppu-smi`。
- 沐曦：`mx-smi`。
- 海光：`hy-smi`；查看进程详情时使用 `hy-smi --showpids`。`htop` 仅用于交互式
  SSH 会话。
- 摩尔线程：`mthreads-gmi`。
- 如果配置的命令不存在，应明确报告，不得静默改用其他厂商的命令。

## 新模型适配工作区

- 本仓库是经过整理的新模型适配工作区，不是一般活动日志。只有当前任务确实涉及新
  模型跑通、硬件平台验证或推理优化时，才能修改或新增适配文件。
- 不得在本仓库或 `models/` 下记录无关的远程运维、临时排查、普通软件包安装、聊天
  内容、探索性命令或未经验证的结论。
- 写入适配记录前，必须明确对应的模型和平台。如果工作没有清晰的模型/平台归属，
  应保持适配文件不变，除非用户明确要求修改仓库级内容。
- 不得仅因为任务已经完成就提交或推送变更。只有用户明确要求时，才能提交或推送。
- 适配工作统一放在 `models/<model-name>/<platform>/` 下。平台目录名必须与 inventory
  组一致：`nvidia`、`ppu`、`metax`、`ascend`、`mthreads` 和 `hygon`。
- 开始为用户指定的平台进行适配时，在该平台目录下直接创建三个工作目录：
  `environment/`、`adaptation/` 和 `acceptance/`。平台根目录只保留作为索引和状态
  元数据的 `README.md` 与 `platform.yml`。环境分析和环境采集文件放入 `environment/`；
  适配分析、参考文件索引、命令、配置、补丁、算子实现、局部测试和过程记录放入
  `adaptation/`；精度、性能和通信测试的配置或包装脚本、简要结果以及最终验收文档放入
  `acceptance/`。
- 使用 `./scripts/new-model <model-name>` 从 `models/_template` 创建新工作区。不得覆盖
  已存在的模型目录。
- 模型级 Tokenizer 工作、公共补丁和一致性测试放在 `_shared` 中。厂商特定文件按照
  内容放入上述三个平台工作目录中的对应目录。
- 执行新的远程适配命令前，先把可重复执行的版本保存到对应平台的 `adaptation/` 目录
  中。不得让聊天或 shell 历史成为该命令的唯一副本。
- 记录准确的主机别名、模型 revision、代码 revision、推理引擎版本、容器镜像、启动
  参数、测试输入和验证日期。
- 每项模型适配任务在完成前，必须创建或更新对应的
  `models/<model-name>/<platform>/README.md`。其中应总结适配范围和结果、遇到的每个
  重要问题、诊断出的原因，以及采用的解决方案或规避措施。必须明确记录尚未解决的
  问题和下一步计划，不得只记录最终成功流程。
- 优化前先建立正确性和性能基线。每次只修改一个实质变量，并保留与基线的对比。
- 保持 `README.md` 和 `platform.yml` 中的状态同步。在验证最小推理前，不得将平台标记
  为 `functional`；在完成正确性回归和性能结果记录前，不得标记为 `optimized`。
- 不得提交模型权重、密钥、完整日志或体积较大的原始性能测试数据。只保存路径和小型
  结果摘要。

## 工作流调用与阶段隔离

- 用户可以按编号或名称指定任意一个或多个阶段：`architecture`（1）、
  `environment`（2）、`adaptation`（3）、`acceptance`（4）和
  `retrospective`（5）。只执行用户选中的阶段；选择多个阶段时，无论用户书写顺序
  如何，都必须按照 1 到 5 的顺序执行。
- 对未选择的前置阶段，只能检查其完整性、时效性、模型与平台身份以及证据，不得自动
  执行或重写。如果必要的前置阶段缺失、过期、失败或信息不一致，应在进入依赖阶段前
  停止并报告准确阻塞原因。
- 用户可以只选择验收中的 `execution-mode`、`sanity`、`accuracy`、
  `performance`、`evidence` 或 `summary` 子步骤。不得执行未选择的验收子步骤；选择
  后面的子步骤不能跳过尚未完成的前置验收条件。
- 使用 `./scripts/adapt-model <model> [--platform <platform> --hosts <aliases>] --steps <steps>`
  只初始化所选阶段缺失的文件、检查结构化前置条件和配置，并生成标准
  Codex 调用文本。步骤 2 至 5 必须通过 `--hosts` 或已有适配配置提供准确的主机别名。
  不允许创建文件时使用 `--check-only`。该工具不会执行远程命令、把阶段标记为通过、
  提交或推送。
- 只有阶段真实达到对应状态且证据文件已经更新后，才能修改 `platform.yml` 中的阶段或
  验收子步骤状态。不得仅根据文件存在就推断为 `passed` 或 `complete`。

## 新模型适配工作流程

1. **模型结构与推理链路分析。** 开始平台适配前，创建或更新
   `models/<model-name>/architecture-and-inference.md`。分析模型整体结构和端到端推理
   链路。该文档必须使用中文编写，包括说明文字、结构分解、算子清单、结论、假设和
   待确认问题；代码符号、配置字段名和已有技术专有名词可以保留原文。分解并列举完整
   的模型结构，包括相关模型配置、主要组件、层级组织、适用时的
   Attention 与 MoE/路由机制、张量和数据流、并行要求、执行阶段，以及实现依赖或兼容
   性风险。同时按照模型组件或执行阶段，列举该模型实际使用的关键算子，例如适用时的
   归一化、投影与矩阵乘、位置编码、Attention 与 Softmax、激活函数与 MLP、路由与专家
   分发/合并、KV Cache、量化/反量化、采样以及集合通信算子。算子结论必须来源于模型
   配置或具体实现，不得直接套用通用架构模板；应明确区分已经验证的事实、假设和尚未
   解决的问题。
2. **推理环境分析。** 对用户明确指定的每个适配平台，在实施平台变更前创建或更新
   `models/<model-name>/<platform>/environment/environment-analysis.md`。相关环境采集脚本
   和输出也放在同一 `environment/` 目录。记录目标主机别名、加速卡型号与拓扑、操作
   系统或容器环境、驱动与运行时、推理框架及平台插件版本、编译器或工具链、可用资源、
   验证命令、兼容性缺口，以及会影响适配方案的环境分析结论。不得为用户未指定的平台
   创建环境分析。
3. **进行适配。** 结合模型结构与推理链路分析、用户指定平台的环境分析，以及用户提供
   的适配相关参考文件，按照以下子步骤完成适配：

   1. **制定 plugin 适配方案。** 基于模型结构与推理链路分析、平台环境分析和用户提供的
      参考文件，将每个模型组件、推理阶段、关键算子、并行要求和运行依赖映射到当前
      plugin 路径及目标平台能力。在 `adaptation/` 中建立差距清单，逐项写明需要修改的
      plugin 位置、算子来源、对 `eager`/`graph` 的影响、依赖、风险和计划验证方式。
   2. **固定基线并明确修改边界。** 在正在运行的适配容器内，修改前记录 plugin、vLLM
      和 FlagGems 的仓库路径、revision、分支及工作区状态。允许直接修改 plugin 源码，
      但整个适配过程中 vLLM 源码必须保持只读且不得产生任何改动。禁止停止、重启或删除
      适配容器。保存 vLLM 修改前后的 revision 与工作区状态证据，并把 plugin 基线信息
      放入 `adaptation/`。
   3. **接入算子前同步 FlagGems。** 检查或接入任何 FlagGems 算子前，先把正在运行的
      适配容器内 FlagGems 仓库更新到预期跟踪分支的最新 commit。更新前记录 remote、
      分支、upstream、revision 和工作区状态。只有工作区干净且目标分支与 upstream 明确
      时才能更新；先 fetch，再使用仅允许 fast-forward 的 pull，禁止 reset、强制更新或
      丢弃本地改动。将同步命令和更新后的 revision 记录到 `adaptation/`。如果同步无法
      完成，应报告准确阻塞原因，不得继续基于旧 revision 选择算子。
   4. **接入 FlagGems 已有算子。** 对 plugin 尚未调用或不支持的每个算子，检查 plugin
      的 dispatch/backend 设计和同步后的 FlagGems revision。如果 FlagGems 已有兼容
      实现，应遵循 plugin 现有架构补齐 dispatch、backend、注册或平台绑定，不得以临时
      直接调用绕过 plugin 设计。补充针对性的接入与数值测试，并在 `adaptation/` 中记录
      FlagGems 符号、revision、plugin 入口、支持约束和测试结果。
   5. **实现 FlagGems 缺失算子。** 如果同步后的 FlagGems revision 没有兼容实现，
      则在 plugin 内新增 Triton 算子，并通过相同的 plugin dispatch 框架接入。Triton
      实现必须兼容 `graph` capture/replay，而不能只支持 `eager`；应避免 capture 阶段
      的主机同步、不受支持的动态内存分配、依赖数据的主机控制流，以及不稳定的张量
      shape 或地址。补充数值、dtype、shape、layout、设备、边界条件和执行模式测试，
      并单独覆盖 `eager` 与 `graph` capture/replay。记录所检查的 FlagGems revision 和
      检索证据，明确标注该算子在 FlagGems 中缺失，并在 `adaptation/` 和最终
      `acceptance/adaptation-summary.md` 中写明 Triton 实现位置、支持约束、plugin
      接入方式、测试结果和尚存限制。
   6. **整理可复现的 FlagGems 算子问题。** 在适配任一阶段，如果 FlagGems 算子出现报错
      或数值精度问题，必须在目标适配镜像所创建且正在运行的适配容器文件系统根目录创建
      `/bug`，并在其中为该问题创建独立目录。不得把该目录创建在当前管理仓库、FlagGems
      仓库、plugin 仓库或 vLLM 仓库的根目录。应在同一适配镜像环境中向算子开发人员提供
      能够准确复现问题、可独立执行的最小单元测试。优先直接测试 FlagGems 算子，不应要求
      启动完整模型服务，也不应依赖 vLLM 或 plugin；如果问题只会通过 plugin dispatch、
      graph capture/replay 或其他必要集成链路触发，则保留能够触发问题的最小链路，并在
      可能时提供直接调用算子的对照测试。固定随机种子，准确记录容器镜像与容器名称、
      FlagGems revision、平台、设备、软件环境、算子参数、dtype、shape、stride/layout、
      执行模式、复现命令、预期行为、实际行为、误差阈值，以及精简的报错或数值差异证据。
      如果问题与执行模式有关，应分别覆盖 `eager` 和 `graph`；精度问题必须提供可信参考
      实现或预期输出。必须在受影响主机的容器内从 `/bug` 执行并验证测试可以稳定复现，
      同时在对应平台的 `adaptation/` 记录中写明容器内问题目录的准确路径和复现命令。
      创建或验证复现用例时不得停止或重启适配容器，也不得在 `/bug` 中存放模型权重、秘密、
      大型日志或与该问题无关的适配产物。
   7. **完成运行配置与集成。** 将并行策略、内存行为、执行阶段、模型配置和启动参数映射
      到目标平台。在不修改 vLLM 源码的前提下，补齐可复现的 plugin 侧配置、包装脚本、
      启动脚本和优化设置，所有产物统一放入 `adaptation/`。启动模型服务时，不得把
      `--max-model-len` 设置得过小；应先根据模型配置和实现确认模型实际支持的最大上下文
      长度。如果最大支持长度大于 50000 token，初始服务配置使用 50000；如果最大支持长度
      小于或等于 50000，则使用模型实际支持的最大长度。无法确认最大支持长度时，不得猜测，
      应先完成核实。把判断依据、计算结果和最终启动参数记录到 `adaptation/`，不得为掩盖
      内存、graph capture 或运行时问题而静默调小该值。
   8. **分层递进验证。** 依次验证 import 与注册、单算子、组件组合、最小模型执行，最后
      拉起完整服务。每次修改后先运行最小相关回归，并在适用时覆盖 `eager` 与 `graph`
      行为；较小范围测试未通过时，不得继续更大范围测试。
   9. **控制适配服务生命周期。** 因配置变更、故障恢复或验证需要时，只能停止或重启当前
      适配任务的推理服务或相关进程。禁止停止、重启或删除适配容器本身。操作前必须准确
      确认服务或进程，不得影响共享或无关服务。
   10. **整理可复现记录。** 持续记录已验证结果、遇到的问题、问题原因、尝试过的修复、
      最终解决方案、未解决风险和下一步计划。进入验收前，把命令、配置、局部测试、
      plugin diff/patch、服务停启记录、FlagGems 证据以及 vLLM 始终未改动的证明统一保存
      到 `adaptation/`。
4. **适配验收。** 完成以下全部验收工作：

   1. **执行模式验收。** 模型必须同时跑通 `eager` 模式和 `graph` 模式。本项只验证两种
      执行模式都能够成功运行；通过后，后续所有验收工作，包括小批量预检、正式精度评测、
      性能评测或 Profiling，以及适用时的通信验证，统一使用已经通过的 `graph` 模式服务
      配置。无需在 `eager` 模式下重复进行精度或性能验收。
   2. **8 并发小批量精度与性能预检。** 正式精度评测前，对每个需要验收的服务配置使用
      一组固定的简单请求，针对已验收的 `graph` 模式服务配置以 8 并发发起小批量验证。
      逐条对照预期结果检查回复，并记录错误、超时、时延、吞吐、加速卡利用率和显存/内存
      使用情况，以识别明显的性能回退。
      如果出现回复错误，先回到适配阶段修复精度；如果回复正确但性能明显异常，必须先
      定位并修复性能问题，再运行全量精度评测。每次修复后都要重新执行本项预检；只有
      精度和性能预检都通过后，才能继续下一项。
   3. **正式全量精度评测。** 在目标机器上，根据目标平台选择进入基于
      `harbor.baai.ac.cn/flageval/flageval-llmeval:v1` 或
      `harbor.baai.ac.cn/flageval/flageval-llmeval:arm64` 镜像的容器，使用
      `test/Accuracy_test/llmrun.py` 对已验收的 `graph` 模式服务执行；除非用户明确要求，
      不得替换为其他评测入口。
      准备模型专用的 `llm_config.json`，先使用 `--preflight-only` 运行 `llmrun.py`，再
      启动正式评测。核对预期样本数、进程完成状态、最终结果与样本文件、精度指标、超时
      数量和明确的通过标准。
   4. **最终性能评测。** 只有第 3 项正式精度评测完成并达到通过标准后，才能使用
      `test/perf_test/` 基于同一套已验收的 `graph` 模式配置开始最终推理性能测试或
      Profiling。全量精度评测尚未完成或未通过时，不得启动正式性能评测。在与平台相关
      时，使用 `test/nccl_test/` 验证通信
      能力，通信验证也应基于 `graph` 模式验收配置。
   5. **验收证据记录。** 在平台的 `acceptance/` 目录中写明准确的测试脚本、配置、数据
      集或用例集、服务模式、容器名称与镜像、执行命令、环境、结果位置、指标、通过标准
      和最终结果，并明确标注第 1 项之后的所有验收均使用 `graph` 模式。大型数据集和原始
      输出保留在本地或远端存储中。如果公共测试文件需要模型专用修改，应在 `acceptance/`
      中放置副本或包装脚本，不得静默修改公共基线。
   6. **最终适配总结。** 创建或更新
      `models/<model-name>/<platform>/acceptance/adaptation-summary.md`，总结适配范围、
      环境、参考文件、实施变更、可复现流程与配置、正确性和性能验证、遇到的问题及解决
      方案、最终状态、尚存限制和下一步计划。只有全部验收项均通过，且该文档准确反映
      已验证结果后，才可以将适配任务视为完成。
5. **适配复盘。** 验收完成后，针对每个已适配平台创建或更新
   `models/<model-name>/<platform>/acceptance/adaptation-retrospective.md`。回顾从模型结构
   与推理链路分析、环境分析、适配实施到验收的完整过程，总结遇到的主要问题及其现象、
   根因、影响、发现阶段、尝试过的方案、最终解决方法、验证证据和尚存影响。纳入容器
   `/bug` 下相关 FlagGems 算子复现问题，并分析是否可以通过更早的分析或更小范围测试提前
   发现。总结值得复用的实践和产物，同时识别重复工作、可避免的弯路、缺失的检查、记录
   不清、工具缺口，以及正确性、性能、`eager` 或 `graph` 覆盖中的不足。必须明确判断当前
   适配流程是否需要继续优化；每项优化建议都要写明证据、预期收益、适用范围、优先级、
   风险，以及建议修改的具体规则、脚本、模板、测试或自动化。区分已验证事实和复盘判断，
   尚未落实的改进应记录为后续事项。不得仅根据复盘结论静默修改仓库通用规则、模板或
   工具，只有用户要求或批准后才能实施。复盘文档完成且与最终适配总结保持一致后，整个
   适配流程才算闭环。

## 验证与报告

- 修改 inventory 或组变量后，运行 `./scripts/inventory`。
- 修改 inventory、变量或 playbook 后，运行 `./scripts/syntax-check`。
- 对多主机操作，应逐台报告结果，并区分未变化、已变化、失败和不可达状态。
- 对未连接或未验证的主机，不得声称操作成功。
- 使用 `./scripts/connectivity-check`，不能只依赖 SSH 退出码；堡垒机可能在报告找不到
  资产的同时返回退出码 0。

## 密钥与敏感信息

- 不得提交密钥。SSH 密钥使用 SSH agent/keychain，其他凭据使用 Ansible Vault 或经
  批准的密钥管理器。
- 包含敏感信息的 Ansible 任务必须设置 `no_log: true`。
