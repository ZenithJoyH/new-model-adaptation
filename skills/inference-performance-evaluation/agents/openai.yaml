interface:
  display_name: "模型适配性能评测"
  short_description: "使用模型适配项目原生工具执行显式操作，校验服务绑定与性能证据"
  default_prompt: "Use $inference-performance-evaluation with an explicit mode to measure for the current inference service."

policy:
  allow_implicit_invocation: true
