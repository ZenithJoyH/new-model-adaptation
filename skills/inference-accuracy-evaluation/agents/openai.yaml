interface:
  display_name: "推理精度评测"
  short_description: "编排新模型适配与端到端优化中的分层正确性回归和正式精度门禁"
  default_prompt: "Use $inference-accuracy-evaluation with an explicit operation to evaluate for the current inference service."

policy:
  allow_implicit_invocation: true
