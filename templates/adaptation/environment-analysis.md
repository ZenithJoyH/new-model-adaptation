# 推理环境分析

## 范围

- 模型：
- 平台：
- 目标 SSH Host 别名：
- 验证日期：

## 用户指定的远端工作目录

| SSH Host 别名 | 用户指定 host_root | 容器名与 container_root | 实际路径/挂载/权限核实证据 |
|---|---|---|---|

- 用户指定目录的原始要求：
- 本轮远端工作目录布局和输出位置（脚本、配置、日志、缓存、结果、tmp；不含源码仓库）：
- 现有目录/文件归属、是否存在其他任务以及无覆盖策略：
- 只读外部模型权重/系统依赖引用：
- 必须目录外写入时的精确路径、用途、用户单独授权（默认无）：
- `07-bugs/` 的容器对应路径：
- `/bug` 如确有工具要求：指向同一 `07-bugs/` 的已批准映射，或明确授权的例外（已有用例不得复制）：

未取得用户指定目录时，只做只读核实，不执行远端新建或写入。进入适配/验收前，在本文
记录每台目标 Host 已确认的 `host_root`、容器及 `container_root`；可执行运行配置放远端
`01-environment/`。本地记录不证明远端目录存在、真实挂载一致或任意程序不会越界。

## 硬件与资源

- 加速卡型号、数量和拓扑：
- 平台规定的查询命令及结果摘要：
- 可用显存、磁盘、主机内存和共享内存：
- 共享或无关工作负载：

## 容器与软件栈

- 适配容器名称和镜像：
- 操作系统及架构：
- 驱动、runtime 和编译工具链：
- Python、PyTorch、Triton/flagtree：
- vLLM 路径、revision、分支和工作树状态：
- Plugin editable-install 证据（包元数据、import 路径、Git 根目录）：
- Plugin 路径、revision、分支和工作树状态：
- FlagGems 路径、revision、分支、upstream 和工作树状态：

## 平台能力与约束

- 支持的 dtype、量化和 attention backend：
- TP/PP、通信库和 graph runtime：
- 已知兼容性缺口：
- 对适配计划有直接影响的环境结论：

## 可重复验证命令

只记录不含密码、令牌、密钥路径或堡垒机详情的命令。环境采集、基线和准备脚本与运行
配置保存在远端 `01-environment/`，本文记录其准确路径、命令和结果摘要；具体适配问题的
诊断/修复命令进入对应的 `../adaptation/NNN-*.md` 问题记录。

## 结论与阻塞项

明确区分已验证结果、推断、阻塞项和下一步。
