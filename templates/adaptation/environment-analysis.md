# 推理环境分析

## 范围

- 模型：
- 平台：
- 目标 SSH Host 别名：
- 验证日期：

## 硬件与资源

- 加速卡型号、数量和拓扑：
- 平台规定的查询命令及结果摘要：
- 可用显存、磁盘、主机内存和共享内存：
- 共享或无关工作负载：

## 容器与软件栈

- 适配容器名称和镜像：
- 操作系统及架构：
- 驱动、runtime 和编译工具链：
- Python、PyTorch、Triton/flagtree：
- vLLM 路径、revision、分支和工作树状态：
- Plugin 路径、revision、分支和工作树状态：
- FlagGems 路径、revision、分支、upstream 和工作树状态：

## 平台能力与约束

- 支持的 dtype、量化和 attention backend：
- TP/PP、通信库和 graph runtime：
- 已知兼容性缺口：
- 对适配计划有直接影响的环境结论：

## 可重复验证命令

只记录不含密码、令牌、密钥路径或堡垒机详情的命令。新远程命令应先保存到
`../adaptation/` 再执行。

## 结论与阻塞项

明确区分已验证结果、推断、阻塞项和下一步。
