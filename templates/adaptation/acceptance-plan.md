# 适配验收计划与证据

除执行模式验收外，后续验收全部使用已通过的 `graph` 模式配置。
本目录只保存 Markdown 计划与结论；脚本、配置、完整日志、缓存和原始结果保存在用户批准
的远端 `03-acceptance/`、`04-runs/`，并在本文记录准确路径和 SHA-256。

## 4.1 执行模式验收

- [ ] `eager` 模式完整服务可以运行
- [ ] `graph` capture/replay 模式完整服务可以运行
- 命令、配置、日志和通过标准：

## 4.2 十并发小批量精度与性能验证

- [ ] 固定简单请求全部正确
- [ ] 10 并发下无错误、超时或明显性能异常
- 并发数：10
- 请求集、期望结果、延迟、吞吐、利用率和显存证据：

## 4.3 全量精度测试

- [ ] 使用 `test/Accuracy_test/llmrun.py`
- [ ] 使用适用的 FlagEval 镜像
- [ ] `--preflight-only` 通过
- [ ] `formal_acceptance=true`、graph、全量样本、并发至少 32、超时样本按错误答案计入分母
- [ ] 每个 task 的精确指标键和阈值已在评测前确定
- [ ] 每个 task 的指标均达到冻结阈值；不要求指标为 1.0，也不要求每题全部答对
- [ ] `acceptance-result.json` 为 passed，配置哈希与本轮配置一致
- [ ] 样本数、输出文件、指标和通过标准已核实
- 配置及结果：
- 配置并发 / 观测有效并发、吞吐、耗时、错误/超时：
- 按仓库 `docs/workflow-guide.md` 中的证据绑定格式更新 phase verification / acceptance records。

## 4.4 正式性能与通信测试

- [ ] 全量精度已经通过
- [ ] 非性能测试保持前缀缓存开启，通用 graph 参数不含关闭参数
- [ ] 性能测试专用服务启动命令包含并生效 `--no-enable-prefix-caching`，且已记录现场证据
- [ ] 使用 `test/perf_test/` 完成性能测试或 profiling
- [ ] 需要时使用 `test/nccl_test/` 完成通信验证
- 基线、变量、指标和结果：

## 4.5 验收证据完整性

- 服务模式、容器、镜像、命令、配置、环境和结果路径：
- [ ] 最终 `start-model.sh` 从最小生产命令整理，而非复制诊断、精度或性能测试命令
- [ ] 已删除可依赖默认值的参数、无关参数和 `--no-enable-prefix-caching`
- [ ] 每个保留的显式参数及环境变量均有必要性依据，并已实际启动验证
- 未完成项和阻塞项：
